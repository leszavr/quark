# Quark UI - Модульный интерфейс чата и блогов

Современный модульный интерфейс для чата и блогов, построенный на Next.js 15 с адаптивным дизайном и продвинутым UX.

## 🚀 Технологии

- **Next.js 15** - React фреймворк с App Router и Turbopack
- **React 19** - Последняя версия React
- **TypeScript 5.6** - Типизированный JavaScript
- **Chakra UI 2.8** - Компонентная библиотека UI
- **Tailwind CSS 3.4** - Utility-first CSS фреймворк
- **Framer Motion 11.0** - Библиотека анимаций и переходов
- **Zustand 4.5** - Легковесное управление состоянием
- **React Query 5.0** - Управление серверным состоянием

## ✨ Основные возможности

### 🎯 Модульный интерфейс
- **ResizableSplitter** - перетаскиваемый разделитель между окнами
- **Адаптивные режимы** - автоматическое переключение между desktop/mobile UI
- **Гибкие пропорции** - сохранение размеров окон между сессиями
- **Режимы просмотра** - home, blog-only, chat-only, both

### 📱 Мобильная навигация
- **Telegram-стиль** навигация для узких экранов
- **Плавные переходы** между списком чатов и переписками
- **Кнопка назад** для возврата к списку чатов
- **Адаптивное переключение** при изменении ширины окна

### 💬 Продвинутый мессенджер
- **Список чатов** с поиском и фильтрами (All Chats, Unread, Channels)
- **Окно переписки** с поддержкой Markdown и файлов
- **Индикация статуса** сообщений (отправлено ✓, доставлено ✓✓, прочитано ✓✓)
- **Онлайн статусы** пользователей с индикаторами
- **Автоматическое изменение** высоты поля ввода

### 📝 Система блогов
- **Лента постов** с красивыми карточками
- **Markdown редактор** для создания и редактирования постов
- **Система комментариев** с поддержкой вложенности
- **Фильтрация и поиск** по категориям и тегам

## 🎨 Дизайн и UX

- **Neomorphic дизайн** с мягкими тенями и объемными элементами
- **Темная/светлая темы** с автоматическим переключением
- **Кастомные шрифты** - Inter (основной) и Space Grotesk (заголовки)
- **Цветовая схема** - градиенты #00f0ff → #00ff88 (фирменные цвета Quark)
- **Полная адаптивность** для мобильных, планшетов и desktop
- **Smooth анимации** с spring-эффектами

## 🛠 Установка и запуск

```bash
# Клонирование проекта
git clone https://git.8ls.ru/odmen/quark_main_ui.git
cd quark_main_ui

# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Запуск продакшн версии
npm start
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере для просмотра результата.

## 📱 Адаптивные режимы

- **Мобильный** (<768px) - однооконный интерфейс с навигацией как в Telegram
- **Планшетный** (768-1024px) - двухоконный режим с фиксированными пропорциями  
- **Desktop** (>1024px) - полный функционал с ResizableSplitter и перетаскиванием

## 🏗 Структура проекта

```
src/
├── app/                    # Next.js 15 App Router
│   ├── globals.css         # Глобальные стили
│   ├── layout.tsx          # Root layout
│   └── page.tsx            # Главная страница
├── components/             # React компоненты
│   ├── AppSidebar.tsx      # Боковая панель навигации
│   ├── BlogFeed.tsx        # Лента блогов с постами
│   ├── ChatList.tsx        # Список чатов с поиском
│   ├── ChatWindow.tsx      # Окно переписки
│   ├── WindowLayout.tsx    # Основной adaptive layout
│   ├── ResizableSplitter.tsx # Перетаскиваемый разделитель
│   └── ThemeToggle.tsx     # Переключатель темы
├── stores/                 # Zustand state management
│   └── appStore.ts         # Главный store приложения
├── theme.ts                # Кастомная тема Chakra UI
└── types/                  # TypeScript определения типов
```

## 🎯 Ключевые компоненты

### WindowLayout
Главный компонент, управляющий отображением окон в зависимости от размера экрана:
- Автоматическое определение мобильного/планшетного/desktop режимов
- Переключение между single-window и multi-window интерфейсами
- Мобильная навигация с анимированными переходами

### ResizableSplitter  
Интерактивный разделитель между окнами блога и чата:
- Визуальные индикаторы при hover (тройные точки)
- Ограничения минимальных (30%) и максимальных (70%) размеров
- Smooth transitions при изменении ширины панелей
- Сохранение пропорций в Zustand store

### ChatList & ChatWindow
Полноценный мессенджер с мобильной навигацией:
- Поиск по чатам и контактам
- Фильтрация (All Chats, Unread, Channels)
- Мобильные переходы: список → переписка → кнопка назад
- Статусы сообщений и онлайн индикаторы пользователей

### AppStore (Zustand)
Централизованное управление состоянием:
```typescript
interface AppState {
  viewMode: 'home' | 'blog-only' | 'chat-only' | 'both'
  splitRatio: number // Пропорции разделения окон (30-70%)
  blogWindow: WindowState // Состояние окна блога
  chatWindow: WindowState // Состояние окна чата
  sidebarCollapsed: boolean
  isAuthenticated: boolean
}
```

## 🔧 Кастомизация

### Тема Chakra UI
Полностью кастомная тема с:
- Neomorphic элементами и мягкими тенями
- Градиентными цветами Quark (#00f0ff → #00ff88)
- Адаптивными breakpoints
- Темной и светлой цветовыми схемами

### Конфигурация Next.js
- **App Router** для современной архитектуры
- **Turbopack** для быстрой разработки
- **TypeScript** конфигурация для строгой типизации
- **Tailwind CSS** интеграция с Chakra UI

## 📋 Roadmap

- [ ] **ИИ-ассистент** с модальным окном и настройками
- [ ] **PWA поддержка** и офлайн режим работы  
- [ ] **Push-уведомления** для новых сообщений
- [ ] **Экспорт данных** (чаты, посты) в различных форматах
- [ ] **Темы оформления** - дополнительные цветовые схемы
- [ ] **Плагиновая система** для расширения функционала

## 🤝 Разработка

Проект разработан с акцентом на:
- **Современные паттерны** React и TypeScript
- **Производительность** и оптимизацию рендеринга
- **Масштабируемость** архитектуры
- **UX/UI качество** на уровне нативных приложений

## 📞 Поддержка

При возникновении вопросов или проблем:
1. Создайте Issue в репозитории
2. Опишите шаги воспроизведения
3. Приложите скриншоты при необходимости

## 📄 Лицензия

MIT License - подробности в файле LICENSE.
