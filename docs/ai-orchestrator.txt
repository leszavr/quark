
```text
Ты — senior AI/ML инженер и full-stack разработчик. Твоя задача — создать микросервис `ai-orchestrator` на **FastAPI + LangChain + Ollama**, который будет управлять ИИ-агентами в системе Quark.

Цель:  
1. Генерировать контент (посты, заголовки, описания)  
2. Поддерживать режим "человек в цикле" — ИИ предлагает, человек одобряет  
3. Интегрироваться с `blog-service` и веб-интерфейсом  
4. Добавить **статус-виджет в веб-интерфейс** для отображения доступности ИИ

Работай в каталоге: `/var/www/quark/services/ai-orchestrator`

---

### 📁 Шаг 1: Инициализация проекта

```bash
cd /var/www/quark/services
mkdir ai-orchestrator && cd ai-orchestrator
python3.11 -m venv venv
source venv/bin/activate
```

Установи зависимости:
```bash
pip install "fastapi[all]" uvicorn langchain langchain-ollama httpx pydantic
```

---

### 🗂️ Шаг 2: Структура проекта

```
ai-orchestrator/
├── src/
│   ├── main.py
│   ├── routers/
│   │   ├── generate.py
│   │   └── approve.py
│   ├── agents/
│   │   ├── writer.py
│   │   ├── summarizer.py
│   │   └── moderator.py
│   ├── schemas/
│   │   └── ai_request.py
│   ├── config/
│   │   └── ollama_config.py
│   └── utils/
│       └── human_approval.py
├── Dockerfile
├── requirements.txt
├── .env
├── .env.example
└── README.md
```

---

### 🧠 Шаг 3: Реализация ИИ-агентов

#### 1. `agents/writer.py`
- Агент: генерирует посты по теме
- Использует `LangChain` + `Ollama` (модель: `llama3`, `mistral`, `phi3`)
- Пример запроса:
  ```python
  "Напиши пост на тему 'Польза сна' в стиле научпоп, 300 слов"
  ```

#### 2. `agents/summarizer.py`
- Сокращает длинный текст до 3-5 предложений

#### 3. `agents/moderator.py`
- Оценивает текст на:
  - Токсичность
  - Спам
  - Нарушение правил
- Возвращает: `{ "verdict": "flagged", "reason": "Спам", "confidence": 0.87 }`

---

### 📡 Шаг 4: Эндпоинты (routers)

#### `POST /ai/generate`
```json
{
  "prompt": "Напиши пост о пользе сна",
  "context": "user_id=123, blog=health-diary",
  "agent": "writer",
  "mode": "draft"
}
```
→ Ответ:
```json
{
  "text": "Сон — основа здоровья...",
  "suggestions": ["Добавить статистику", "Смягчить тон"],
  "requires_approval": true,
  "ai_model": "llama3",
  "cost_estimate": 0
}
```

#### `POST /ai/approve`
```json
{
  "request_id": "abc",
  "approved": true,
  "edited_text": "Обновлённый текст..."
}
```
→ Если одобрен — отправляет в `blog-service` или `messaging-service`

---

### 🔐 Шаг 5: Аутентификация

- Все эндпоинты требуют `Authorization: Bearer <token>`
- Проверка JWT (общий секрет с `auth-service`) или вызов `/auth/validate`

---

### 🐳 Шаг 6: Dockerfile

```Dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "3006"]
```

---

### 🔄 Шаг 7: Обновление docker-compose.yml

Добавь в `/var/www/quark/docker-compose.yml`:

```yaml
  ai-orchestrator:
    build: ./services/ai-orchestrator
    ports:
      - "3006:3006"
    environment:
      - OLLAMA_HOST=http://ollama:11434
      - JWT_SECRET=${JWT_SECRET}
    depends_on:
      - ollama
      - auth-service
    restart: unless-stopped
```

---

### 🖼️ Шаг 8: Интеграция с веб-интерфейсом

> 🔥 Ты должен **обновить `/var/www/quark/playground/index.html`**, чтобы добавить:
- **Статус ИИ** в шапке
- **Форму генерации поста**
- **Окно предварительного просмотра**
- **Кнопку "Одобрить" / "Отклонить"**

#### 1. В шапку добавь:
```html
<div id="ai-status" class="mt-2 text-sm">
  <span class="dot bg-gray-400"></span> ИИ: подключение...
</div>
```

#### 2. В вкладку "Тестирование" добавь:
```html
<!-- Генерация поста -->
<h3 class="font-semibold mt-6">🤖 Сгенерировать пост через ИИ</h3>
<input id="aiPrompt" placeholder="Тема поста" class="border p-2 w-full mb-2" />
<button onclick="generatePost()" class="bg-teal-600 text-white px-4 py-2 rounded">Сгенерировать</button>
<div id="aiResult" class="mt-2 p-3 bg-gray-50 rounded hidden">
  <p id="aiText"></p>
  <div class="mt-2">
    <button onclick="approveAI()" class="bg-green-600 text-white px-3 py-1 rounded text-sm mr-2">✅ Одобрить</button>
    <button onclick="rejectAI()" class="bg-red-600 text-white px-3 py-1 rounded text-sm">❌ Отклонить</button>
  </div>
</div>
```

#### 3. Добавь JavaScript в `script.js` или `<script>`:

```javascript
// Проверка статуса ИИ
async function checkAIStatus() {
  try {
    const res = await fetch('http://localhost:3006/health');
    if (res.ok) {
      document.getElementById('ai-status').innerHTML = 
        '<span class="dot bg-green-500"></span> ИИ: доступен (llama3)';
    }
  } catch (e) {
    document.getElementById('ai-status').innerHTML = 
      '<span class="dot bg-red-500"></span> ИИ: недоступен';
  }
}

// Генерация поста
async function generatePost() {
  const prompt = document.getElementById('aiPrompt').value;
  const token = localStorage.getItem('quark_token');
  
  const res = await fetch('http://localhost:3006/ai/generate', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${token}`
    },
    body: JSON.stringify({ prompt, agent: 'writer', mode: 'draft' })
  });
  
  const data = await res.json();
  const resultEl = document.getElementById('aiResult');
  document.getElementById('aiText').innerText = data.text;
  resultEl.classList.remove('hidden');
  window.aiLastResponse = data;
}

// Одобрение
function approveAI() {
  const token = localStorage.getItem('quark_token');
  fetch('http://localhost:3006/ai/approve', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${token}`
    },
    body: JSON.stringify({
      request_id: window.aiLastResponse.id,
      approved: true,
      edited_text: document.getElementById('aiText').innerText
    })
  }).then(() => {
    alert('Пост одобрен и отправлен в блог!');
    document.getElementById('aiResult').classList.add('hidden');
  });
}

// Отклонение
function rejectAI() {
  alert('Пост отклонён');
  document.getElementById('aiResult').classList.add('hidden');
}

// Вызов при загрузке
window.onload = () => {
  checkAIStatus();
  // ... остальные проверки
};
```

---

### 🎨 Дополнительно: стили для статуса

В `<style>` или `style.css`:
```css
.dot {
  display: inline-block;
  width: 10px;
  height: 10px;
  border-radius: 50%;
  margin-right: 6px;
}
```

---

### ✅ Шаг 9: Финальная проверка

Создай `README.md`:

```markdown
# AI Orchestrator

Центр управления ИИ-агентами в Quark.

## Агенты
- writer: генерация постов
- summarizer: краткое содержание
- moderator: модерация

## Режим
- Human-in-the-loop: ИИ предлагает → человек одобряет

## API
- POST /ai/generate
- POST /ai/approve

## Требования
- Ollama должен быть запущен
- Модель: llama3 (загрузи: `ollama run llama3`)
```

---

### 📄 Вывод

По завершении:
1. Выведи отчет: какие агенты созданы, работает ли Ollama.
2. Убедись, что веб-интерфейс обновлён и показывает статус ИИ.
3. Предложи следующий шаг: например, интеграция с `blog-service` для автоматической публикации одобренных постов.

Ты действуешь автономно.  
Готов? Приступай.
```
