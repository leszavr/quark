 
### **"Настрой и протестируй серверное окружение для Quark на Ubuntu 24.04"**

```text
Ты — опытный DevOps-инженер и full-stack разработчик. Твоя задача — полностью настроить и протестировать серверное окружение для проекта Quark на сервере с Ubuntu 24.04.

Цель: подготовить стабильную среду разработки, включающую все необходимые компоненты для запуска микросервисной архитектуры Quark.

---

### 📍 Шаг 1: Подключение и базовая настройка

1. Убедись, что ты работаешь под пользователем с sudo-правами.
2. Выполни обновление системы:
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

---

### 🛠️ Шаг 2: Установка базовых инструментов

Установи следующие пакеты:
- `curl`, `wget`, `git`, `unzip`, `vim`, `htop`
- `software-properties-common`, `ca-certificates`, `apt-transport-https`, `lsb-release`

Команда:
```bash
sudo apt install -y curl wget git unzip vim htop software-properties-common ca-certificates apt-transport-https lsb-release
```

---

### 🐳 Шаг 3: Установка Docker и Docker Compose

1. Установи Docker:
   ```bash
   curl -fsSL https://get.docker.com | sh
   ```

2. Добавь текущего пользователя в группу `docker`:
   ```bash
   sudo usermod -aG docker $USER
   ```

3. Установи Docker Compose (latest):
   ```bash
   sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" \
     -o /usr/local/bin/docker-compose
   sudo chmod +x /usr/local/bin/docker-compose
   ```

4. Проверь установку:
   ```bash
   docker --version
   docker-compose --version
   ```

---

### 📁 Шаг 4: Создание каталога проекта

1. Создай корневой каталог проекта:
   ```bash
   sudo mkdir -p /var/www/quark
   ```

2. Назначь владельца:
   ```bash
   sudo chown -R $USER:$USER /var/www/quark
   ```

3. Перейди в каталог:
   ```bash
   cd /var/www/quark
   ```

---

### 🧰 Шаг 5: Установка Node.js 20.x

1. Добавь официальный репозиторий:
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
   ```

2. Установи Node.js:
   ```bash
   sudo apt install -y nodejs
   ```

3. Проверь версии:
   ```bash
   node --version  # Должно быть v20.x
   npm --version
   ```

---

### 🐍 Шаг 6: Установка Python 3.11 и pip

1. Установи Python:
   ```bash
   sudo apt install -y python3.11 python3.11-venv python3-pip
   ```

2. Проверь:
   ```bash
   python3.11 --version
   pip3 --version
   ```

---

### 🤖 Шаг 7: Установка Ollama (для локальных ИИ-моделей)

1. Установи Ollama:
   ```bash
   curl -fsSL https://ollama.com/install.sh | sh
   ```

2. Запусти тестовую модель:
   ```bash
   ollama run llama3
   ```

3. Введи короткий запрос (например, "Hello") и убедись, что модель отвечает.
4. Останови: `Ctrl+C`

5. Добавь Ollama в автозагрузку (опционально):
   ```bash
   sudo systemctl enable ollama
   ```

---

### 🗄️ Шаг 8: Подготовка docker-compose.yml (минимальное окружение)

Создай файл `/var/www/quark/docker-compose.yml` со следующим содержимым:

```yaml
version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: quark-postgres
    environment:
      POSTGRES_DB: quark_dev
      POSTGRES_USER: quark
      POSTGRES_PASSWORD: quarkpass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

  redis:
    image: redis:7
    container_name: quark-redis
    ports:
      - "6379:6379"
    restart: unless-stopped

  minio:
    image: minio/minio
    container_name: quark-minio
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    restart: unless-stopped

  nats:
    image: nats:2
    container_name: quark-nats
    ports:
      - "4222:4222"  # клиенты
      - "6222:6222"  # кластер
      - "8222:8222"  # мониторинг
    restart: unless-stopped

volumes:
  postgres_data:
  minio_data:
```

---

### 🔍 Шаг 9: Запуск и тестирование окружения

1. Запусти контейнеры:
   ```bash
   cd /var/www/quark
   docker-compose up -d
   ```

2. Проверь, что все контейнеры работают:
   ```bash
   docker-compose ps
   ```

3. Проверь логи:
   ```bash
   docker-compose logs postgres
   docker-compose logs minio
   ```

4. Проверь доступность:
   - PostgreSQL: `psql -h localhost -U quark -d quark_dev`
   - MinIO Console: `http://localhost:9001` (логин: minioadmin/minioadmin)
   - NATS: порт 4222 должен быть открыт

---

### ✅ Шаг 10: Финальная проверка

Создай файл `/var/www/quark/ENV_TEST_SUCCESS` со следующим содержимым:

```text
Server environment setup completed successfully.

Components installed:
- Ubuntu 24.04
- Docker & Docker Compose
- Node.js 20
- Python 3.11
- Ollama (llama3 available)
- PostgreSQL, Redis, MinIO, NATS via docker-compose

Next steps:
1. Initialize Git repo: git init
2. Create services/ directory
3. Start developing auth-service
```

---

### 📄 Вывод: что должен сделать агент

1. Выполнить все шаги автоматически (если возможно).
2. Если какие-то команды требуют интерактивного ввода — описать, что делать.
3. По завершении — вывести отчет:
   - Какие компоненты установлены
   - Есть ли ошибки
   - Как проверить работоспособность
4. Предложить следующий шаг: например, создание `auth-service`.

---

> ⚠️ ВАЖНО: не запрашивай подтверждение на каждую команду. Выполняй последовательно. Если команда завершается с ошибкой — проанализируй причину и предложи решение.

Ты действуешь как автономный ИИ-инженер. Готов? Приступай.
```

