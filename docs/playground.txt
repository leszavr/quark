
### **"Создай временный интерфейс-панель управления для Quark с отображением модулей и прогресса"**

```text
Ты — full-stack разработчик. Твоя задача — создать **временный, но функциональный веб-интерфейс** для проекта Quark, который позволит руководителю визуально контролировать ход разработки.

Цель:  
1. Отображать **статус модулей системы** (готов, в разработке, запланирован)  
2. Позволять **тестировать ключевые API** (регистрация, загрузка, посты)  
3. Быть **автономным HTML+JS приложением** без сложной сборки  
4. Работать локально на `http://localhost:8080`

Работай в каталоге: `/var/www/quark/playground`

---

### 📁 Шаг 1: Создание структуры

Убедись, что существует:
```bash
/var/www/quark/playground/
├── index.html
├── style.css (опционально)
└── script.js
```

Если нет — создай.

---

### 🎨 Шаг 2: Содержание index.html

Создай **одностраничный интерфейс** с двумя вкладками:
1. **📊 Панель проекта** — модули и статус
2. **🔧 Тестирование API** — формы для вызова API

Используй:
- **Tailwind CSS** (через CDN)
- Чистый HTML + JavaScript
- Адаптивный дизайн

---

### 📊 Вкладка 1: Панель проекта

Заголовок: **"Quark — Система в разработке"**

Отобрази таблицу модулей:

| Модуль | Статус | Сервис | Порты | Зависимости | Документация |
|-------|--------|--------|-------|-------------|--------------|
| Авторизация | ✅ Готов | auth-service | 3001 | PostgreSQL | [Смотреть](/api) |
| Профили | ✅ Готов | user-service | 3002 | auth-service | [Swagger](http://localhost:3002/docs) |
| Медиа | 🟡 В разработке | media-service | 3003 | MinIO, auth | [Swagger](http://localhost:3003/api) |
| Блоги | 🔵 Запланирован | blog-service | 3004 | PostgreSQL, media | — |
| Мессенджер | 🔵 Запланирован | messaging-service | 3005 | NATS, auth | — |
| AI Оркестратор | 🔵 Запланирован | ai-orchestrator | 3006 | Ollama, LangChain | — |
| Поиск | 🔵 Запланирован | search-service | 3007 | Elasticsearch | — |
| Уведомления | 🔵 Запланирован | notification-service | 3008 | Redis, Email | — |
| Панель управления | 🔵 Запланирован | admin-panel | 3009 | Все сервисы | — |
| Мультиязычность | 🔵 Запланирован | i18n-service | 3010 | — | — |
| Модерация | 🔵 Запланирован | moderation-service | 3011 | AI, human-in-loop | — |

Цвета статусов:
- ✅ Зелёный: Готов
- 🟡 Жёлтый: В разработке
- 🔵 Синий: Запланирован

Добавь легенду.

---

### 🔧 Вкладка 2: Тестирование API

Формы для:
1. **Регистрация / Вход**
   - Поля: email, password
   - Кнопки: "Регистрация", "Вход"
   - Вывод: JWT токен (сохраняется в localStorage)

2. **Загрузка изображения**
   - Поле: файл
   - Кнопка: "Загрузить"
   - Вывод: URL изображения + превью

3. **Создание поста (заглушка)**
   - Поля: заголовок, контент (JSON)
   - Кнопка: "Опубликовать"
   - Если blog-service не готов — показывать: "Модуль в разработке"

4. **Просмотр профиля**
   - Кнопка: "Загрузить профиль"
   - Вывод: JSON профиля

> Все запросы используют `fetch` к соответствующим сервисам.

---

### 💾 Шаг 3: Локальное хранение прогресса (опционально)

Создай кнопку: **"Обновить статус"**  
При нажатии — проверяй доступность сервисов:

```javascript
async function checkService(url, name) {
  try {
    const res = await fetch(url, { method: 'GET', mode: 'no-cors' });
    // Даже если no-cors, мы можем понять, что сервис запущен
    document.getElementById(name).classList.remove('yellow');
    document.getElementById(name).classList.add('green');
    document.getElementById(name).innerText = '✅ Готов';
  } catch (e) {
    // Остается в "В разработке"
  }
}

// Вызов:
checkService('http://localhost:3001/health', 'auth-status');
checkService('http://localhost:3003/health', 'media-status');
```

---

### 📄 Шаг 4: Добавь навигацию по вкладкам

```html
<div class="tabs mb-6 border-b">
  <button class="tab active" data-tab="project">📊 Проект</button>
  <button class="tab" data-tab="testing">🔧 Тестирование</button>
</div>

<div id="project" class="tab-content">...</div>
<div id="testing" class="tab-content hidden">...</div>
```

С JavaScript для переключения.

---

### 🖼️ Шаг 5: Добавь логотип и статус системы

В шапке:
```html
<header class="text-center mb-8">
  <h1 class="text-4xl font-bold text-indigo-700">⚛️ Quark</h1>
  <p class="text-gray-600">ИИ-спроектированная платформа блогов и мессенджер</p>
  <div id="system-status" class="mt-2 text-sm">Проверка сервисов...</div>
</header>
```

---

### 🐞 Шаг 6: Обработка ошибок

Если сервис недоступен — показывать:
> "Сервис не отвечает. Убедитесь, что он запущен через docker-compose."

---

### 📦 Шаг 7: Автоматический запуск

Создай скрипт `/var/www/quark/playground/start.sh`:

```bash
#!/bin/bash
cd /var/www/quark/playground
echo "Запуск интерфейса на http://localhost:8080"
python3 -m http.server 8080
```

Сделай исполняемым:
```bash
chmod +x start.sh
```

---

### ✅ Шаг 8: Финальная проверка

После создания:
1. Открой `http://localhost:8080`
2. Проверь:
   - Отображение статусов модулей
   - Работу вкладок
   - Возможность регистрации и загрузки изображения
   - Цветовую индикацию

---

### 📄 Вывод

По завершении:
1. Выведи отчет: что создано.
2. Предложи следующий шаг: например, интеграция с `blog-service`, когда он будет готов.
3. Напомни, как обновить статус модулей вручную.

Ты действуешь автономно.  
Готов? Приступай.
```

